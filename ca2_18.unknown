#!/bin/bash
echo Script for finding file is readable and writable
read -p "Enter the file name:" file
if [ -r "$file" ] && [ -w "$file" ]; then
    echo "The file is both readable and writable."
else
    echo "The file is not both readable and writable."
fi

